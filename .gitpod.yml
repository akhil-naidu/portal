tasks:
  - name: NextJS Frontend
    init: yarn install && yarn run build
    command: yarn run start
  - name: Hasura Backend
    before: cd hasura
    init: docker-compose up -d
ports:
  - port: 3000
    onOpen: open-preview
  - port: 5432
  - port: 8080
  - port: 9693
  - port: 9695
    onOpen: open-browser
